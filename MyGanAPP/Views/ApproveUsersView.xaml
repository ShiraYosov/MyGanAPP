<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyGanAPP.Views.ApproveUsersView"
             FlowDirection="RightToLeft">
    <ContentPage.Content>

        <CollectionView x:Name="collectionName" 
                            ItemsSource="{Binding TeachersList}" SelectionMode="Single"
                        SelectionChangedCommand="{Binding SelctionChanged}"
                        SelectionChangedCommandParameter="{Binding Source={x:Reference collectionName}, Path=SelectedItem}" Margin="0">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.LeftItems>
                            <SwipeItems SwipeBehaviorOnInvoked="RemainOpen">

                                <SwipeItemView BackgroundColor="#e1eff6">
                                    
                                </SwipeItemView>

                                <SwipeItemView BackgroundColor="#c8e7ff">
                                    
                                    
                                </SwipeItemView>
                            </SwipeItems>
                        </SwipeView.LeftItems>

                        <StackLayout>
                            <Grid Padding="20" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" BackgroundColor="LightGray">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*"></RowDefinition>
                                    <RowDefinition Height="auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <!--<Label Grid.Column="0" Grid.Row="0"
                                   Text="שם הפעילות" FontAttributes="Bold"/>
                            <Label Grid.Column="1" Grid.Row="0"
                                   Text="{Binding ActivityName}"/>
                            <Label Grid.Column="0" Grid.Row="1"
                                   Text="עיר" FontAttributes="Bold"/>
                            <Label Grid.Column="1" Grid.Row="1"
                                   Text="{Binding City}"/>
                            <Label Grid.Column="0" Grid.Row="2"
                                   Text="רחוב" FontAttributes="Bold"/>
                            <Label Grid.Column="1" Grid.Row="2"
                                   Text="{Binding Street}"/>-->



                                <Frame Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Style="{StaticResource SmallImageFrame}">
                                    <Image Source="{Binding PhotoURL}" Style="{StaticResource SmallImage}"/>
                                </Frame>

                            </Grid>
                            <BoxView HeightRequest="1.5" WidthRequest="2000" BackgroundColor="Black" Margin="0"></BoxView>

                        </StackLayout>
                        


                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </ContentPage.Content>
</ContentPage>